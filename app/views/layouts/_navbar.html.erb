<nav class="navbar navbar-expand-lg bg-body-tertiary bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand text-white" href="#">Asra Metal Works</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to "Home", root_path, class:"nav-link text-white" %>
        </li>
        <li class="nav-item">
          <%= link_to "Browse", root_path, class:"nav-link text-white" %>
        </li>

        <% unless current_admin %>
            <li class="nav-item">
                <%= link_to "Admin", new_admin_session_path, class:"nav-link text-white" %>
            </li>
        <% end %>

        <% if current_admin %>
            
            <li class="nav-item">
                <%= button_to "Sign Out", destroy_admin_session_path, method: :delete, class:"nav-link text-white", style:"background:none; border:none;" %>
            </li>

            <li class="nav-item">
                <%= link_to "Add A Product", new_product_path, class:"nav-link text-white" %>
            </li>
      
        <% end %>
      </ul>
      
    </div>
  </div>
</nav>

